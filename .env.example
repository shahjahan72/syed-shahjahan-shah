# Example environment variables (DO NOT COMMIT REAL CREDENTIALS)

# PostgreSQL / Neon (connection string format)
# Use the full connection string provided by Neon, e.g.:
# postgresql://<user>:<password>@<host>:<port>/<database>?sslmode=require
DATABASE_URL=

# Optional separate parts (useful for some libraries or debugging)
DATABASE_HOST=
DATABASE_PORT=5432
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_NAME=

# App / deployment
APP_URL=https://printifystudio.pk

# Supabase / Postgres (Prisma)
# DATABASE_URL: runtime connection string used by Prisma client
# DIRECT_URL: use this for migrations if using PgBouncer/connection pooling in Supabase
DATABASE_URL=
DIRECT_URL=

# Admin auth token for admin routes (keep secret)
ADMIN_TOKEN=

# Supabase (Client-side public keys - DO NOT commit real values to the repository)
# Vercel: set both NEXT_PUBLIC_* and VITE_* variants if you're using Vite so they are available on the client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NOTE: For deployments (Vercel), configure these as Project Environment Variables or Secrets.
# For serverless apps, use connection pooling or Prisma Data Proxy for stable connections.
